# The engine core and the text demo (play.py) run on the Python standard
# library only (Python 3.8+).
#
# The graphical inventory screen (gui.py) needs pygame:
pygame>=2.5

# Development (optional):
#   pip install pytest
